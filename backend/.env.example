# Deepgram API Key (optional - users provide their own via the extension UI)
# Only needed if you want a server-side fallback key
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Server port
PORT=3001

# Environment
NODE_ENV=development

# Logging level (error, warn, info, debug)
LOG_LEVEL=info
